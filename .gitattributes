# Vendored third-party bundle — never normalize line endings or diff it.
js/vendor/** binary linguist-vendored

# Binary assets
*.woff2 binary
*.png binary
*.jpg binary

# Design source — approved Claude Design artboards. Reference material, not
# application code: keep it out of language stats and out of routine diffs.
design/** linguist-documentation -diff
